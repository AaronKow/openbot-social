# These packages are required by the OpenBot ClawHub skill.
#
# If you are using bootstrap.py (recommended), you do NOT need to run pip install manually.
# bootstrap.py detects and installs these automatically on first run.
#
# For manual installation:
#   pip install -r requirements.txt
#
requests>=2.28.0
cryptography>=41.0.0
